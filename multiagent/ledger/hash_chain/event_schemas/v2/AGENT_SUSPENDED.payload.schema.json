{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://alagf.io/schemas/v2/events/AGENT_SUSPENDED.payload.schema.json",
  "title": "AGENT_SUSPENDED event payload",
  "description": "Payload for AGENT_SUSPENDED ledger events. Governance rationale: Invariant 3 (Evidence-First) — suspension must reference the prior registration event; no state changes without traceable cause.",
  "type": "object",
  "additionalProperties": false,
  "required": ["agent_id", "reason", "prior_registration_event_id"],
  "properties": {
    "agent_id": {
      "type": "string",
      "minLength": 1
    },
    "reason": {
      "type": "string",
      "minLength": 1,
      "description": "Human-authored justification for suspension."
    },
    "prior_registration_event_id": {
      "type": "string",
      "pattern": "^evt_[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "ULID of the AGENT_REGISTERED event being suspended."
    }
  }
}
